# Master task list

Read [README.md](README.md) for the ID scheme, states and priorities, and
[GLOSSARY.md](GLOSSARY.md) for the vocabulary. Contradictions a person must
settle, and items with no owner, are in [DECISIONS.md](DECISIONS.md).

Compiled 2026-08-05 from nine planning documents, which were then deleted; see
[README.md](README.md) for where each one's content went. Duplicates have been
merged — where three documents described one job, the row names all three and
keeps the most current description. Items the staleness audit found already done
are in **Closed** at the bottom with their evidence, not deleted.

Re-audited against the tree 2026-08-09. Rows the code shows are built carry
state `done` and the commit that built them, in place; rows whose description
cited a file or symbol that has since moved carry the correction in their
provenance, marked with the date. A row is only `done` where the source says so
— several that look finished are still open with the reason written down.

---

## RAS — raster

The G track owns most of this. [DESIGN.md](DESIGN.md) holds each step's shape and
acceptance criteria; these rows are the index into it.

| ID | Title | St | Pri | Eff | Owner | Blocked by | Provenance |
|---|---|---|---|---|---|---|---|
| RAS-001 | **G6c — PBR evaluates the tracer's material derivation, not stand-ins** | **done** | P1 | M | phase-f G6c | — | `735fccd69` 2026-08-08: the chain is `slang/lib/material_ops.slang` — decode, curated per-object ops, category override, roughness scale and floor — imported at `pbr_resolve.slang:48` and evaluated at `:325-333`, by the same call the tracer's hit path, the tracer's G-buffer path and the debug views make. DESIGN.md; retro-vs-PBR deltas 6.1%→57.2% |
| RAS-002 | G6c — retire the Odyssey range cull and falloff from PBR only | decided | P1 | S | phase-f G6c | RAS-001 (done) | DESIGN.md; decided 2026-08-05, unbuilt. **Citation corrected 2026-08-09**: the radius² cutoff is `pbr_resolve.slang:442-448` ("kept exactly as the OpenGL shader has it") and the non-inverse-square falloff `:240-241` |
| RAS-003 | **G8 — the blended pass and the three alpha kinds** | open | P1 | L | phase-f G8 | — | DESIGN.md; backlog 5.4-adjacent. Tracer side is gated on the same work: `scene_draw.slang:38,85-86` publishes cutout coverage only, in lockstep with `tracing/trace.slang`'s candidateAlpha/commitsCoverage. **Corrected 2026-08-09**: the module was `lib/megadraw_geometry.slang`, renamed in `fedcb7445`; and `4980518cc` runs raster's blended pass in the traced tail too, so additive layers no longer wait on G8 in path tracing |
| RAS-004 | G8 — classification: use the authored `transparencyHint` as the blended signal | open | P1 | M | phase-f G8 | RAS-003 | FIDELITY #21, #2; `mdlmdxreader.cpp:475` parsed and unread |
| RAS-005 | G8 — per-node alpha fade must actually render | open | P1 | M | phase-f G8 | RAS-004 | FIDELITY #18. **Premise re-checked 2026-08-09**: the cited `scene_resolve.slang:301` no longer exists, and the node's alpha controller does now reach the material record — `admission.cpp:220-222` carries it in `diffuseColor.a` and `scene_draw.slang:423` multiplies `input.color.a * material.diffuseColor.a`. Whether the fade renders is therefore no longer answerable from the source: it needs FIDELITY's fade fixture (a placeable driven 1→0, captured at three values) before this row is either closed or re-aimed |
| RAS-006 | G8 — the transparency sort remap (two reads: index and material) | open | P1 | M | phase-f G8 | RAS-003 | DESIGN.md; contradicts reference correction 8 → see DECISIONS |
| RAS-007 | G8 — authored emitter `renderOrder` in the ordering decision | open | P2 | S | phase-f G8 | RAS-006 | FIDELITY #11; 1,904 K1 / 1,014 K2 non-zero |
| RAS-008 | G8 — additive without alpha uses `SRC_COLOR/ONE` | open | P2 | S | phase-f G8 | RAS-003 | FIDELITY #4; phase-f reference correction 7 |
| RAS-009 | G8 — `Punch-Through` emitter blend is dropped | open | P2 | S | phase-f G8 | — | FIDELITY #22; `emitter.cpp:325` tests only `Lighten` |
| RAS-010 | **G9 — the shared output stage: bloom, lens flares, anti-aliasing** | open | P1 | L | phase-f G9 | RAS-003 | DESIGN.md; merges FIDELITY #6, #7, backlog 4.2, vulkan-remaining §3. **Partly built 2026-08-09**: the tail exists and every mode runs it — an anti-aliasing slot then the one display transform (`8fc369e97`), the scene linear until post (`7ffdf8d8c`), sharpen a stage of its own after the transform (`d943d5d1a`). Anti-aliasing is therefore discharged (see RAS-011, RAS-012). Bloom is RAS-033, in the slot immediately before the display transform; lens flares are the remainder and have no row of their own |
| RAS-011 | G9 — decide `ENABLE_FSR` default and Linux AA | **done** | P1 | M | phase-f G9 | RAS-010 | `490df9461` 2026-08-09: `ENABLE_FSR` defaults to ON under MSVC and OFF elsewhere (`CMakeLists.txt:52-57`), because enabling it off-MSVC trips the FATAL_ERROR at `:170-172` — so Linux AA is the FXAA slot or none. The row's premise that "PT skips the step list" died with `8fc369e97`: one AA slot (off/fxaa/fsr, `optionsregistry.cpp:350-381`) serves every mode. backlog 4.2 |
| RAS-012 | G9 — path tracing shows no image difference with jitter on vs off | **done** | P1 | S | phase-f G9 | — | `9accfeddd` 2026-08-06 settled it by measurement and removed the dial: jitter is applied exactly when FSR occupies the common slot, 77% of G-buffer depth moves between a jittered and an unjittered frame, 0% between a frame started in FXAA and one switched to FXAA. The option, its enum, `--taajitter` and the editor combo are gone. DESIGN.md |
| RAS-013 | Bump has never been held to a fixture | open | P2 | S | phase-f G6 | — | DESIGN.md; the one part of G6's scope never discharged |
| RAS-014 | Metal at LOD 0 filters nothing at distance; minified metal may alias | open | P3 | S | none | — | DESIGN.md, left open deliberately |
| RAS-015 | Adopt textured roughness/metalness in one step for both consumers | open | P2 | M | none | RAS-001 (done) | DESIGN.md. **Citations corrected 2026-08-09**: `lib/surface.slang` no longer exists and the `tracing/material.slang:219` note was replaced when the chain moved (`735fccd69`). One stand-in site remains and it is now shared rather than duplicated — `pbr_resolve.slang:149-153`, "Odyssey materials carry no roughness or metallic channel", roughness 1.0 and metallic 0.0 before `lib/material_ops.slang` applies the curated ops, the category override and the roughness scale/floor. Curation (`override/k1/materials.ini`) is the current substitute for authored channels; adopting real textures still has to move both consumers at once, but now through one file |
| RAS-016 | Sky cubemap keeps only 0.73 of the geometry sky's horizontal detail | open | P2 | M | none | — | backlog 8.11; residual is resampling, not resolution |
| RAS-017 | Collapse per-body-part registration (30 draws for one droid) | open | P2 | M | none | — | backlog 9.4, RECORD.md; raster-only now |
| RAS-018 | Per-pass instance re-copy in the draw walk | open | P2 | S | none | — | backlog 9.5 |
| RAS-019 | SSAO — unowned, decide | **done** | P3 | M | none | — | `fedcb7445` 2026-08-06: the PBR resolve became a compute dispatch and occlusion computes inside it from the depth and normals it has already read — `pbr_resolve.slang:222-230`, consumed at `:346` and `:514-517` in place of the hard-coded `ao = 1.0`. Gated by a live `ssao` option (`optionsregistry.cpp:341-343`); off, the frame is the frame from before. DESIGN.md |
| RAS-020 | SSR — unowned, decide | **done** | P3 | M | none | — | `fedcb7445` 2026-08-06: a second compute dispatch over the resolved image, substituting for the authored cube at the same `1 - alpha` weight rather than adding to it; live `ssr` option at `optionsregistry.cpp:344-346`. Both effects moved out of `postprocess.slang`, where they had sat unwired. DESIGN.md |
| RAS-022 | Far-hills shadow breakdown: cascade seam + undersampled far cascades | open | P2 | M | none | — | 2026-08-06, user-spotted in the V2 gallery, measured: shadowres 1→4 changes 72% of hills-band pixels (vs 21% frame-wide), and a 12.9-luma step sits exactly at the cascade-1→2 boundary (depth 37.5 of divisors {0.005,0.015,0.045,0.135}×far). Pre-existing in both raster modes; candidates are divisor retuning, seam blending, and a beyond-last-cascade decision |
| RAS-026 | Configurable maximum number of shadow-casting dynamic lights | open | P2 | L | none | — | 2026-08-06, user-requested. Exactly one light casts today: `SceneGraph::updateShadowLight` takes `computeClosestLights(1, ...)` (`graph.cpp:302-304`) and the pipeline allocates one 4-layer cascade array plus one 6-face cube (`scenepipeline.cpp:120-128`), with `globalUniforms.shadowLightSpace` and a single `shadowStrength`/`shadowLightPosition` behind it. Making the count configurable touches all four: N shadow maps (layered arrays or an atlas - decide, since a cube per point light is 6 faces each), an N-entry uniform table replacing the singleton, a receiving loop over N in `lib/shadow.slang` where `getShadow` returns one visibility today, and the shadow pass recording N times with the per-light caster policy. Belongs in the reapply class of the options registry, since it decides what the pipeline allocates. Watch the cost: the pass already draws every opaque surface with cull disabled in PBR, so N doubles or triples that; and the fade in `updateShadowLight` cross-fades between shadow lights, which only means something for a set rather than a single latched light. **2026-08-09**: still one caster (`graph.cpp:346`), but the budget decision is now recorded where the pipeline will read it — `options.h:200-210` carries `maxDirectionalShadows {2}` and `maxPointShadows {8}` with "Not read yet" said in as many words |
| RAS-031 | Warp and save-load need a renderer reset to render correctly | open | P1 | M | none | — | 2026-08-06, user-observed: after a warp or a load the frame is wrong until the renderer is reset (which `gfx apply` now provides as a workaround). A module change calls `renderer.invalidateResources()` and `SceneGraph::clear()` (`game.cpp:598-603`) but never rebuilds the render pipeline, so everything the pipeline owns outlives the module it was built for. Two concrete lifetime bugs found by inspection, both of the kind the comment at `game.cpp:599` warns about for the renderer: (1) `SceneGraph::clear()` (`graph.cpp:114-132`) clears the light containers but never resets `_shadowLight` (`graph.h:413`), a raw `LightSceneNode *` into the freed scene - `hasShadowLight()` stays true and `shadowLightPosition()`/`isShadowLightDirectional()` dereference it, so shadows for the new module come from a dangling pointer until a new light happens to replace it; (2) `Sky` keys its bake on the room node's ADDRESS (`renderpipeline.cpp:142` casts the pointer to `_skyCubeRoom`, tested at `sky.cpp:84`), and a new module can allocate a different room at a reused address, in which case the bake is skipped and the previous module's sky cube is kept. Also unreset across the change: `_shadowStrength`, the admission classification cache and its generation, the device-side GpuScene, and the tracer's acceleration structures and bindless registry - audit each. The fix is not simply to rebuild the pipeline on every module load (that would throw away work the incremental scene exists to keep); it is to give the pipeline an explicit scene-change reset and make identity-by-address impossible - a generation counter or a stable id rather than a raw pointer. **Both named lifetime bugs are fixed, the row is not**: `SceneGraph::clear` now resets `_shadowLight`, `_shadowActive` and `_shadowStrength` with the dangling read written out in the comment (`graph.cpp:114-140`), and the sky bake keys on `room.identity`, a `uint64_t` (`sky.h:93`, `sky.cpp:84-87`), so a reused address cannot pass for the same room. What remains is the general case the row is actually about: a module change still does not rebuild the pipeline, and the admission classification cache, the device-side GpuScene and the tracer's acceleration structures are still unaudited across it. `gfx apply` reaches the reset through `Engine::applyGraphicsRebuild` (`engine.cpp:736-759`) |
| RAS-032 | Stale GL-era premises in the shader headers | open | P3 | S | none | — | 2026-08-06, found while sweeping the shaders for deferred work. **Line numbers refreshed 2026-08-09; both premises survive the shader consolidation.** `uniforms.slang:168-175` still reasons about a GLSL target - "Slang emits `set = N` into GLSL, which is not valid GLSL", so blocks and textures "share one number space when targeting SPIR-V" and "will need distinct ranges once Vulkan lands". Vulkan has landed, the sets are distinct, and the GL backend is gone, so the comment now teaches the opposite of the truth. `postprocess.slang:17-28` has the same shape: it keeps FXAA and sharpen as a faithful port "so the two backends can be compared numerically", a reason that expired with the GL backend. One clause of this row is itself now stale - sharpen does have a consumer, a pass of its own after the display transform (`d943d5d1a`; `scenepipeline.h:76,296`, option at `options.h:668`). Sweep the shader headers for premises that named the two-backend era and correct or delete them; a comment that argues from a dead constraint is worse than none |
| RAS-033 | Bloom in the shared post-process pass | open | P2 | M | none | — | 2026-08-06, user-requested, to follow the shader consolidation. The common tail already has the slot: `postProcessFragment` in `postprocess.slang` runs after anti-aliasing and transparency and owns the one display transform, so bloom belongs immediately before that transform, on linear colour, not after it. Wants a bright-pass plus a separable or downsample/upsample chain, which means extra targets and therefore the reapply option class in the registry - and the traced mode's scene output is already R16G16B16A16Sfloat, so its highlights survive; the raster output is the swapchain format and clips at one, so decide whether raster bloom needs a float intermediate or accepts a clipped source. Note RAS-010 (G9 shared output stage) names bloom and lens flares together as the original's pair; take the flare question with it or explicitly defer. Reference for what the original bloomed: xoreos/KotOR.js at C:\Development\odessey |
| RAS-030 | Animated grid textures outside the bumpmap slot show one wrong frame (Taris sewer force fields) | open | P1 | M | none | RAS-028 | 2026-08-06, user-observed: the force-field door texture in the Taris sewers is the wrong image. Mechanism found, not yet confirmed on the asset: any texture whose TXI names a procedure type with numX/numY > 1 is converted from a grid sheet into a layered array at load (`resource/provider/textures.cpp:100-105`, `convertGridTextureToArray`), whatever slot it is destined for - but the only consumer of a cycle frame is `MeshSceneNode::updateBumpmapAnimation` (`mesh.cpp:125-139`), which returns immediately unless the node has a BUMPMAP, and the frame reaches the material solely as `bumpMapFrame` on the `BumpMapArray` slot (`mesh.cpp:296-300`). An animated DIFFUSE - force fields, water, scrolling energy - therefore becomes an N-layer array that nothing ever indexes, so it renders as a single fixed layer or a mis-sampled array rather than the authored animation. Confirm by dumping the force-field material's diffuse and its TXI, then extend the cycle to every slot that can carry one, sharing the stepping fix from RAS-028 rather than duplicating it. Secondary suspect if that is not it: the MDL mesh header carries four texture names and the reader keeps two - texture3 and texture4 are read and discarded (`mdlmdxreader.cpp:210-211`), and texture2 is treated as a lightmap unconditionally (`:466-467`) with no check that it is one |
| RAS-027 | Particle spawning is frame-quantised, so emitter density follows frame rate | open | P2 | M | none | — | 2026-08-06, user-observed. `EmitterSceneNode::spawnParticles` (`emitter.cpp:128-145`) spawns at most ONE particle per frame per emitter: the birth timer elapses, one `doSpawnParticle()` runs, and the timer resets to a full `_birthInterval`, discarding the overshoot. Any emitter whose birthrate exceeds the frame rate - a birthrate of 100/s needs 3.3 spawns per frame at 30 fps - emits at the frame rate instead, so density and the look of every fountain emitter change with performance, and the discarded remainder makes spawn timing drift. Fix: spawn `floor(accumulated / interval)` particles and carry the remainder, with a sane per-frame cap for the case where a long hitch would otherwise dump hundreds at once. Same file's Lightning branch has the identical shape |
| RAS-028 | Texture cycle animation is choppy: rounded frame index and a doubled last frame | open | P2 | S | none | — | 2026-08-06, user-observed. `MeshSceneNode::updateBumpmapAnimation` (`mesh.cpp:125-139`) advances a cycle time by dt, clamps it to the cycle length, then picks the frame with `round((frameCount - 1) * (time / length))`. Rounding gives the first and last frames half the on-screen duration of every other frame, and the reset depends on `_bumpmapCycleTime == length` exactly - true only because of the clamp, which holds the final frame for an extra update. Should be `floor(time * fps) % frameCount` with the time wrapped by modulo, so every frame is held equally and the cycle never depends on a float equality. Check the same pattern wherever procedural Cycle textures are stepped |
| RAS-029 | Frame-timed animation behaviour that should be game-timed | open | P2 | M | none | RAS-027, RAS-028 | 2026-08-06, user-observed: "some animations don't use the actual game timer, but are frame timed". The two instances found so far are RAS-027 and RAS-028; a third is `MeshSceneNode::updateDanglyAnimation` (`mesh.cpp:145-150`), which clamps dt into 0.0125..0.035 outright, so dangly motion runs fast or slow with frame time by construction. Sweep the scene layer for the rest - anything advancing state per update rather than per elapsed second, or clamping/discarding dt - and decide the policy: game time drives simulation, frame time drives only interpolation. Note the capture harness pins frameTime to 1/60 (`engine.cpp:391`), which hides every one of these in captures and is why they only show up in play |
| RAS-024 | Settings exposes the pipeline's shape, not the three render modes | **done** | P2 | S | none | — | 2026-08-06, user-spotted; built by `fedcb7445` 2026-08-06. One three-way control: `Editor::renderModeCombo` (`editor.cpp:1679-1710`) shows Retro/PBR/Path tracing, live between the two raster modes and staged for Apply on either crossing into or out of path tracing, and the reapply note follows the value rather than the option (the `applyFor` hook at `optionsregistry.cpp:234-253`). The row proposed keeping `mode` and `pbr` separate and adding a front door; the change went further and collapsed them - `RenderMode{Retro,PBR,PathTracing}` in `options.h:120-125` is the single option, `--mode retro`/`pbr`/`path-tracing` is the flag, `--pbr` is gone, and the console reaches it as `gfx set mode <value>` |
| RAS-025 | Retro renders emissive textures as plain white | open | P2 | M | none | — | 2026-08-06, user-observed. Self-illuminated surfaces come out white in the retro resolve instead of carrying their authored emissive colour. Suspects, in order: the resolve adds `selfIllumSample.rgb` after the albedo multiply as an unmodulated term (`retro_resolve.slang`, reference-exact change of eeb22ffe8) so a bright self-illum saturates to white where the original tinted it; and the G-buffer's self-illum channel may carry the material's selfIllumColor rather than an emissive texture at all - check what `scene_draw.slang` writes to `selfIllumColor` and whether an authored emissive map ever reaches it. Compare against the path tracer, which has its own emission path, and against xoreos/KotOR.js for what the original multiplied |
| RAS-023 | Dantooine's sun fades in over the first 30 frames | open | P2 | S | none | — | 2026-08-06, user-observed. `LightSceneNode::_strength` starts at 0 and ramps at `kFadeSpeed = 2.0`/s when the MDL light carries the `fading` flag (`light.cpp:47-65`, flag read at `mdlmdxreader.cpp:525`), and it multiplies the light's own multiplier - so a fading sun ramps the whole module's direct lighting for half a second, 30 frames at the capture run's fixed 1/60 s step. The shadow term already has an explicit startup exception for exactly this symptom (`graph.cpp:331`: starting dim "only makes the module visibly brighten during its opening frames"); the lights themselves never got one. Decide whether the ramp should apply on level entry at all, or only to lights that switch on during play. Note for the capture harness: `--captureframe` below ~30 lands on the ramp, and the default is 3 |
| RAS-021 | Measure phase F on low-end hardware; per-mesh draws may win | open | P3 | M | none | — | RECORD.md; "raster keeps per-mesh draws" is an acceptable answer |

| RAS-034 | Grass size ramp fades from a distance the cull no longer reaches | open | P2 | S | none | — | 2026-08-09, found in the documentation sweep. `slang/scene_resolve.slang:475` scales blade size by `saturate((32.0 - distance(position, pushConstants.cameraPosition.xyz)) / 4.0)` — a fade beginning 32 units out. The cull is no longer at 32: `GraphicsOptions::grassRadius` defaults to 25 (`options.h:222`) and is measured to face bounds (`gpuscene.cpp:567-625`), so every admitted blade already sits inside the ramp's full-size region and the fade never runs. Blades reach the cull at full size and disappear. The constant is a survivor of `kMaxClusterDistance`, which `1c9e261d2` deleted along with the rest of the old grant scheme. Drive the ramp from the live radius or delete it; FIDELITY #32 |
| RAS-035 | Two texture upload paths disagree about mips for compressed formats | **closed** | P1 | S | none | — | **Not a defect; closed 2026-08-09 on inspection, the same day it was filed.** The two conditions differ in text and agree in effect. The 2D path at `resources.cpp:422` is the `else` of `if (auto compressed = compressedFormat(texture.pixelFormat()))` at `:386`, so a block-compressed 2D texture takes the authored-levels branch and never reaches it; the `!compressed` term would be redundant. The cube/layered path at `:347` serves both formats in one block and so has to spell the test out. Filed from reading the two conditions side by side without their enclosing scopes, which is also how it would be re-filed — a comment at `:422` now records why the term is absent. Original finding: 2026-08-09, documentation sweep |

## STR — scene and structural

The S track. [DESIGN.md](DESIGN.md) part 5 holds the step designs and the seven
decisions already taken.

| ID | Title | St | Pri | Eff | Owner | Blocked by | Provenance |
|---|---|---|---|---|---|---|---|
| STR-001 | **S0 — sub-zone `SceneGraph::update` and `Game::update`** | open | P1 | S | S0 | — | DESIGN.md; 1.25 ms is uninstrumented. **Half done as of 2026-08-09**: the scene side carries zones - `SceneGraph::update` and four sub-zones under it (`graph.cpp:239,265,287,304`), plus collection, admission and the per-stream updates. `Game::update` has none: `R_PROFILE_ZONE` does not appear anywhere in `src/libs/game`. Note the zones compile to nothing unless `ENABLE_TRACY` is on, which now defaults OFF (`490df9461`) |
| STR-002 | S0 — the scaling fixture (the no-creep tripwire) | open | P1 | M | S0 | — | DESIGN.md |
| STR-003 | S0 — update the diagnostics skill's calibration line | open | P2 | S | S0 | STR-001 | DESIGN.md; stale by 46×/4× |
| STR-004 | **S2 — nodes own GPU slots (this is R2)** | open | P1 | L | S2 | RAS-003 | DESIGN.md; merges phase-f R2, backlog 4.8, 9.2 |
| STR-005 | S2 — fix `SceneGraph::_nodes` never releasing | open | P1 | M | S2 | — | backlog 6.2; a module transition leaks the whole node graph |
| STR-006 | S3 — skin palettes become event-driven | open | P2 | M | S3 | STR-004 | DESIGN.md; 61 meshes × 128 bones unconditionally |
| STR-007 | S3 — dangly moves into the merge kernel | open | P2 | M | S3 | STR-004 | DESIGN.md; absorbs backlog 3.5; deletes 789 calls/frame |
| STR-008 | S3 — particles simulate on the GPU | open | P2 | L | S3 | STR-004 | DESIGN.md; answers backlog 7.4's granularity question |
| STR-009 | S3 — `settleMeshTransform` dies | open | P2 | S | S3 | STR-004 | DESIGN.md |
| STR-010 | S3 — flare LOS becomes a GPU visibility query | open | P2 | M | S3 | RAS-010 | DESIGN.md; coordinate with G9's flare un-filtering |
| STR-011 | S4 — decide what "static" is provable from | open | P1 | S | S4 | — | backlog 9.1; the admission proof, not the authored hint |
| STR-012 | S4 — static/dynamic partition of merged buffer and material table | open | P1 | L | S4 | STR-004, STR-011 | DESIGN.md; danm14ab 45k of 86k static |
| STR-013 | S4 — remove the per-element binary search in the merge | open | P3 | S | S4 | STR-012 | backlog 8.12 |
| STR-014 | S5 stage 1 — a layout-tracking image type | **done** | P2 | S | S5 | — | `65a9ff7f`; barriers 56→38, 3 latches deleted; DESIGN.md; collapses 5 helpers + ~65 barrier sites |
| STR-015 | S5 stage 1 — a `RenderPassScope` RAII type | **done** | P2 | S | S5 | — | `65a9ff7f`; 55 sites → 0; DESIGN.md; ~182 preamble lines |
| STR-016 | S5 stage 1 — a pipeline builder covering compute and RT | **done** | P2 | S | S5 | — | `65a9ff7f`; creation calls 32→18; DESIGN.md |
| STR-017 | S5 stage 1 — a descriptor-write builder | **done** | P2 | S | S5 | — | `65a9ff7f`; 4 files → 0/0; DESIGN.md; ~1,100 lines out for ~350 in |
| STR-018 | S5 stage 2 — formalise the RHI seam | **done** | P2 | L | S5 | — | `b1087464`..`eec7c0be`; both gates zero; done without TRC-020, which proved a stability preference not a dependency; DESIGN.md; grep gate on `Vk`/`vk`/`vma` outside the RHI |
| STR-019 | S6 — delete the legacy per-mesh path | blocked | P3 | M | S6 | TRC-024 | DESIGN.md; last consumer is the runtime sky bake |
| STR-020 | S6 — delete `Registered*` and the frame-phase surface | blocked | P3 | S | S6 | STR-004 | DESIGN.md |
| STR-021 | S6 — delete the per-draw texture-set path | blocked | P3 | M | S6 | descriptor indexing | **premise corrected 2026-08-05**: DESIGN.md said this trails "once 2D rides the RHI". 2D now does, and it changed nothing — `acquireTextureSet` has five live callers (`pbrtextures` ×2, `renderer2d`, `scenepipeline`, `tracingpipeline`). Its own comment names the real blocker: it is the placeholder for descriptor indexing, and removing it means one bindless array indexed per draw, which changes how every shader declares its textures. Not a deletion; a feature. |
| STR-022 | **Room visibility: the VIS graph is gone** | open | P1 | M | none | — | FIDELITY #17; `.vis` parsed and never read; see DECISIONS |
| STR-023 | Debug geometry returns inside `GpuScene`, in a non-BLAS region | open | P2 | M | none | — | backlog 7.8; the per-triangle material id makes `offMaterial` a non-blocker. Unblocked 2026-08-09: TRC-020 is done (`4980518cc`) |
| STR-024 | The `admitted` vs `drawn` distinction has no mechanism under ranges | open | P3 | S | none | — | RECORD.md |
| STR-025 | Walkmesh geometry deletion is a game-model refactor | open | P3 | M | none | STR-023 | RECORD.md |
| STR-026 | Frame floor outside the renderer (the ~2 ms update slot) | open | P2 | M | none | STR-001 | backlog 8.8 |
| STR-027 | In-phase wind: every dangly tree moves identically forever | open | P3 | M | none | STR-007 | RECORD.md; `0.01*abs(sin(_windTime))` along world X |
| STR-028 | S5 stage 3 — move the five RHI clients out of `vulkan/` | **done** | P2 | L | S5 | — | `06db928e`; 2026-08-05 RHI sweep; 4,020 lines leave; `vulkan/` 9,398 → 5,378 `.cpp` lines |
| STR-029 | S5 stage 3 — **no `Vulkan` identifier may appear outside `vulkan/`** | **done** | P1 | M | S5 | — | `9c62f8aa`..`eec7c0be`; verified by both gate lines; 2026-08-05 RHI sweep; 85 occurrences, 13 type names, incl. `IVulkanSceneCallbacks` and `scene/render/pipeline/vulkan.cpp` |
| STR-030 | S5 stage 3 — parent class outside, `Vulkan*` child inside | **done** | P2 | M | S5 | — | 18 parents under `include/reone/graphics/`; 2026-08-05 RHI sweep, revised same day; supersedes the interface-collapse reading |

## TRC — path tracing

The PT substage runs after the raster track. [DESIGN.md](DESIGN.md) part 3 holds the
substage design; [RECORD.md](RECORD.md) holds the traced-transparency ladder.

| ID | Title | St | Pri | Eff | Owner | Blocked by | Provenance |
|---|---|---|---|---|---|---|---|
| TRC-001 | **Smoke particles produce non-finite pixels; the denoiser spreads them** | open | P0 | M | none | — | backlog 1.13; lead is the three disagreeing transmission caps (16/12/uncapped) |
| TRC-002 | **NRD accumulation is nondeterministic** | open | P1 | M | none | — | backlog 7.5; sd 0.14043 with NRD vs 0.00063 without; all three suspects negative. Two things changed around it in 2026-08: RELAX joined REBLUR as a selectable denoiser (`1e58b2760`, `options.h:99-104`) so the run can be repeated against a second implementation, and `ENABLE_NRD` now defaults OFF (`490df9461`), which means a default build cannot reproduce this at all |
| TRC-003 | Run the FSR convergence harness and record the numbers | open | P0 | S | none | — | backlog 0.1; claimed done three times, verified zero |
| TRC-004 | Record why growing `InstanceMaterial` caused `VK_ERROR_DEVICE_LOST` | open | P0 | S | none | — | backlog 0.3; hazard is storage-buffer array stride |
| TRC-005 | Verify specular demodulation on chromatic `Rf0` | open | P1 | S | none | — | backlog 0.5; needs the metalness scale to force the case |
| TRC-006 | Residual diffuse demodulation leak (corr 0.145, was 0.271) | open | P1 | M | none | — | backlog 1.1 |
| TRC-007 | Specular `F` vs `Fenv` mismatch | open | P1 | M | none | — | backlog 1.2; same bug class diffuse had |
| TRC-008 | Diffuse channel outliers (max 25.3 vs p99.99 1.30) | open | P2 | S | none | — | backlog 1.3; firefly candidate under motion |
| TRC-009 | FSR reactive and transparency/composition masks are null | open | P1 | M | none | — | backlog 1.4; additive blades and particles are exactly these cases |
| TRC-010 | Light selection table evaluated twice per path vertex | open | P2 | S | none | — | backlog 1.10; caching 32 weights halves it |
| TRC-011 | Sphere lights sampled over the whole cone, not the visible cap | open | P3 | M | none | — | backlog 1.11 |
| TRC-012 | Lightmaps treated as albedo where physically wrong | open | P3 | M | none | — | backlog 1.8; revisit once real GI exists |
| TRC-013 | Feed NRD `IN_DIFF_CONFIDENCE` / `IN_SPEC_CONFIDENCE` | open | P1 | M | none | — | backlog 2.1 |
| TRC-014 | Feed NRD `IN_DISOCCLUSION_THRESHOLD_MIX` | open | P2 | M | none | — | backlog 2.2; aimed at foliage edges |
| TRC-015 | Evaluate NRD's SH path + `NRD_SG_ReJitter` | open | P3 | L | none | — | backlog 2.3 |
| TRC-016 | Channel debug views need their own exposure | open | P2 | S | none | — | backlog 2.4 |
| TRC-017 | **Material PBR-ification: authored name→material map, then heuristic** | open | P1 | L | none | — | backlog 3.2, RECORD.md; order is load-bearing. **Mechanism landed, content has not (2026-08-09)**: `TraceMaterialOverrides` keys curated materials by model/node (`include/reone/scene/render/pipeline/tracematerials.h`), `mesh.cpp:322-323` resolves the index, `admission.cpp:239-265` writes the ops into the material record, and `lib/material_ops.slang` evaluates authored-first-then-derived in both consumers - mode 0 leaves the heuristic alone, 1 overrides, 2 drives the channel from albedo. What is missing is curation: `override/k1/materials.ini` holds 2,914 lines, `override/k2/materials.ini` one |
| TRC-018 | An editor mode to maintain the authored material map | **done** | P2 | M | none | TRC-017 | `bf9c9e6e1`, `76bcdf9fe`: the "Curated material" window (`editor.cpp:2048-2103`) edits class, albedo multiplier, the roughness and metallic mode ladders and emission for the picked model/node, applies live and writes through `TraceMaterialOverrides::setCurated`, which persists. RECORD.md |
| TRC-019 | **Traced transparency: stochastic commits, flat additive loop, analytic sabers** | decided | P2 | L | PT substage | RAS-003, TRC-020 | backlog 3.7; design settled 2026-08-03, unbuilt |
| TRC-020 | **V1 — hybridise: raster owns primary visibility (V1a/b/c)** | **done** | P1 | L | phase-f V1 | — | DESIGN.md; V1a settled by measurement, V1b `eebcf1d3` 2026-08-06, **V1c `4980518cc` 2026-08-06**: `path_trace.slang:201` calls `ptReadPrimary` instead of casting a camera ray, and `slang/tracing/primary.slang` reconstructs the surface from the G-buffer's triangle id — the geometric normal from that triangle's own vertices, the material from `mergedMaterialIds`. Only secondary, shadow and continuation rays are traced. The material-id target went with it |
| TRC-021 | Pin the traced G-buffer as the `RTDebug` validation instrument | open | **P0** | M | none | — | backlog 7.9. **The window this row was racing has closed (2026-08-09).** It was written to pin the instrument *before* V1c removed the traced primary; V1c landed in `4980518cc` and this did not. The four canonicals still publish (`tracingpipeline.cpp:237-249`, bindings at `tracing/outputs.slang:49-54`), but they are now filled from the primary the tracer *read out of the raster G-buffer* — `path_trace.slang:254-258` writes them from `primaryShading`, which `primary.slang:158` samples from `gbufDiffuse`. So the two agree by construction and the comparison proves nothing: **traced geometry has no independent correctness oracle.** P1→P0 and S→M: the job is no longer to pin a live instrument but to restore one (a camera-ray path behind a debug switch, or an offline reference), and P0 is the right class because the surviving channels read as agreement and can be mistaken for validation. The `RTDebug` *mode* named here never existed in code; debug views are one option over G-buffer channels in every mode (`debug_view.slang:69-90`), with traced-only channels showing a hazard card |
| TRC-022 | The fog grid, the march, and additive as spheres/capsules | open | P2 | L | PT substage | TRC-019 | DESIGN.md-1150; census 714bd700 |
| TRC-023 | Honour the authored emitter `tinted` flag when baking media | open | P2 | S | PT substage | TRC-022 | DESIGN.md; 1,124 emitters carry it |
| TRC-024 | **V0/V2/V4/V5 — the curated offline sky chain** | open | P2 | L | phase-f V | RAS-010 | backlog 1.14, DESIGN.md; merges four steps + FIDELITY #1 |
| TRC-025 | V0 — sky coverage curation (every entry still marked `# review`) | open | P2 | L | phase-f V0 | TRC-024 | DESIGN.md; 56/117 K1, 46/82 K2 |
| TRC-026 | ReSTIR DI | open | P2 | L | PT substage | TRC-019 | DESIGN.md; current loop is one-sample RIS with no reuse. TRC-020 dropped from the blockers 2026-08-09: done (`4980518cc`) |
| TRC-027 | SHARC radiance cache, then resample into a dense volume | open | P3 | L | PT substage | TRC-026 | DESIGN.md-1185 |
| TRC-028 | Volumetrics consuming the grid (prototype transmittance first) | open | P3 | L | PT substage | TRC-022, TRC-027 | DESIGN.md |
| TRC-029 | Next-event estimation over the full light list / light BVH | open | P1 | L | none | TRC-026 | backlog 1.5, RECORD.md |
| TRC-030 | Emissive geometry into the sampled light set | open | P2 | L | none | TRC-029 | RECORD.md |
| TRC-031 | Geometric grass instead of alpha-tested quads | **done** | P2 | L | none | — | `1c9e261d2` 2026-08-07, extended by `c8e104251`: a blade is nine triangles generated in the merge kernel from a hash of its identity, so the field is bit-identical frame to frame; clusters are granted on the CPU against a triangle ceiling while it already walks the faces (`gpuscene.cpp:568-640`), sized to exactly the blades drawn. Strands are ordinary opaque geometry, which is the point — every shadow ray crossing a grass field used to run the any-hit loop and alpha-test a texture per blade. Retro keeps the cardboard (`options.h` grass block), and pays nothing for it, being raster-only. backlog 2.6 |
| TRC-032 | Explain 7.2% occupancy on the primary-ray dispatch | open | P2 | M | none | TOOL-002 | backlog 8.7 |
| TRC-033 | `HitGeometry` and `SurfaceShading` are the remaining live state | open | P2 | M | none | — | backlog 8.13; occupancy still 22.7% |
| TRC-034 | BLAS compaction is not implemented | open | P2 | S | S4 | — | backlog 8.14; every quoted memory figure is post-compaction |
| TRC-035 | **Verify AMD's Vulkan BLAS build path before trusting per-frame rebuild** | open | P2 | M | S4 | — | backlog 8.15; 7.4× gap would consume the frame budget |
| TRC-036 | Is 86k triangles typical? Log counts across a warp loop | open | P3 | S | none | — | backlog 8.16 |
| TRC-037 | Re-trace the current renderer; the old 12 ms predates the merge | open | P2 | M | none | TOOL-002 | backlog §8 preamble |
| TRC-038 | SER, once a Slang release emits the instruction | blocked | P2 | M | none | Slang toolchain | backlog 8.10; measured speedup was noise |
| TRC-039 | Sky leaks into fully enclosed scenes (Taris underground) | open | P2 | M | none | TRC-024 | RECORD.md |
| TRC-040 | Bounce lighting does not exist for analytic lights | open | P2 | M | none | TRC-029 | RECORD.md; NEE runs at the primary hit only |
| TRC-041 | Some objects do not interact with lighting (leaves, hair, doors) | open | P2 | M | none | — | RECORD.md |
| TRC-042 | Calibration programme: tonemapper, intensities, bounces, sky scale | open | P2 | M | none | — | RECORD.md; recorded 2026-07-29 from a live review |
| TRC-043 | Ambient light has no place in path tracing | decided | P3 | S | none | — | RECORD.md |
| TRC-044 | Multiple importance sampling between specular and diffuse lobes | open | P3 | M | none | — | RECORD.md |
| TRC-045 | A distributable denoiser (A-SVGF) if NRD's licence blocks shipping | open | P3 | L | none | — | rt-backend §12.1 fallback. The conditional has resolved: `490df9461` 2026-08-09 makes `ENABLE_NRD` default OFF "because its license cannot be distributed here" (`CMakeLists.txt:45`), so the shipped default configuration has no NRD in it. `8611ae52b` then moved blue noise, the shadow-filter image and the denoiser-kind flag out of the NRD guard, since a build without NRD did not compile |
| TRC-046 | Ray-traced shadows / AO / reflections replacing the raster equivalents | open | P3 | L | none | — | rt-backend §10.3. Unblocked 2026-08-09: TRC-020 is done (`4980518cc`) |

| TRC-048 | NRD is fed a world-space penumbra width where it reads a normalized hit distance | open | P1 | M | none | — | 2026-08-09, found while fixing TRC-047. `slang/tracing/outputs.slang:198` writes `float4(directDiffuse, penumbra)`, and its own comment called alpha free because a constant had been there. It is free only while `slang/shadow_filter.slang:85` is the reader. Under `ShadowFilter::Denoiser` the same image is handed to NRD as `inputs.directRadianceHitDist` (`tracingpipeline.cpp:461`, `aux[14]`), and the second instance is `RELAX_DIFFUSE`/`REBLUR_DIFFUSE` (`nrddenoiser.cpp:57-59`), whose `IN_DIFF_RADIANCE_HITDIST` reads `.w` as a **normalized** hit distance. What arrives is `shadow.occluderDistance * tan(angular)` (`tracing/lighting.slang:219`) — a world-space penumbra width, unbounded and in metres — so NRD sizes its kernel from a quantity in the wrong units and the wrong range. Decide the fix: normalize the penumbra into the slot NRD expects, write a real hit distance and carry the penumbra elsewhere, or give the direct channel a second target. Changes what the denoiser path looks like, so it wants a capture pair before and after |
| TRC-047 | `outputs.slang` says the direct channel never reaches a denoiser; it does | **done** | P3 | S | none | — | Fixed 2026-08-09: the comment predated `f5639b444` and claimed the channel was "nothing a denoiser reads", which stopped being true when direct light got its own NRD instance. Corrected at `slang/tracing/outputs.slang:55-64`, and the alpha-slot collision it exposed is TRC-048. Original finding: 2026-08-09, found in the documentation sweep. `slang/tracing/outputs.slang:55-58` documents `outDirectDiffuse` as "Direct diffuse light at the primary vertex, which does not go through NRD … nothing a denoiser reads". Under `ShadowFilter::Denoiser` it does: `tracingpipeline.cpp:444-445` returns `_nrdDenoiser->denoisedDirect()` for that channel. The comment predates `f5639b444` "Direct light gets its own denoiser". Correct the comment — or the routing, if the comment is the intent and the wiring is the mistake |

## FID — retro fidelity

`FIDELITY.md` holds all 37 rows with their evidence, statuses
and required proofs. Only the rows with no owner elsewhere are repeated here, so
the two files do not drift. **Everything in that file is in scope; this is the
index, not a subset.**

| ID | Title | St | Pri | Eff | Owner | Blocked by | Provenance |
|---|---|---|---|---|---|---|---|
| FID-001 | Alpha-test reference value (0.5 vs the reference 0.1) | open | P1 | S | phase-f G8 | RAS-003 | FIDELITY #19 |
| FID-002 | The opaque range is untested and reaches it too easily | unproven | P1 | M | phase-f G8 | RAS-003 | FIDELITY #20; needs a content scan first |
| FID-003 | Emitter fidelity set: tint, rotation, size, order, jitter, alignment | open | P2 | L | S3 | STR-008 | FIDELITY #8–#14, #22 |
| FID-004 | Light selection policy (32 global, no per-model cap, no frustum cull) | unproven | P2 | M | none | — | FIDELITY #23; treat 8/3 as one reimplementation's model |
| FID-005 | Dynamic light on lightmapped geometry | unproven | P2 | M | none | — | FIDELITY #24 |
| FID-006 | `SunShadows` / `MoonShadows` parsed and ignored | open | P2 | S | none | — | FIDELITY #25. Still ignored, but no longer only parsed: `area.cpp:249-257` puts both into `ShadowProperties` and `:378` hands that to the scene graph, which reads `opacity` alone (`graph.cpp:599`). The two flags reach the consumer and stop there, so the remaining work is a policy decision, not plumbing |
| FID-007 | No shadow or soft-shadow option | unproven | P3 | S | none | — | FIDELITY #26; the ini proves the settings existed, not the technique |
| FID-008 | Fog distance metric is radial, references are planar | unproven | P3 | S | none | — | FIDELITY #27; one-line change once a capture exists |
| FID-009 | Anisotropic filtering on by default (4×) | unproven | P2 | S | none | — | FIDELITY #28; check the retail ini and KVP sampler state first |
| FID-010 | TXI `filter` and `mipmap` flags unparsed | open | P2 | S | none | — | FIDELITY #29 |
| FID-011 | BC textures with no shipped mip chain get none | unproven | P3 | S | none | — | FIDELITY #30 |
| FID-012 | Object draw distance is dead code and the slider does nothing | open | P2 | S | none | — | FIDELITY #31; supersedes backlog 3.6 |
| FID-013 | Grass distance policy (camera vs player, 32 vs 25→100, no dialogue exemption) | unproven | P2 | M | phase-f R3 | — | FIDELITY #32 |
| FID-014 | Destroy fade ignored (`bNoFade`, `fDelayUntilFade`) | open | P2 | S | none | RAS-005 | FIDELITY #33 |
| FID-015 | Meshes with a null diffuse texture are dropped | unproven | P3 | S | none | — | FIDELITY #34; needs a content scan |
| FID-016 | Self-illumination operator: multiply vs add | unproven | P2 | S | none | — | FIDELITY #35; the references disagree |
| FID-017 | Face culling: G-buffer and blended two-sided, shadow pass culls | unproven | P3 | S | none | — | FIDELITY #5 |
| FID-018 | Shadow-caster scope: the authored per-node flag exists and is unread | open | P2 | M | phase-f G7 | — | FIDELITY #16 |
| FID-019 | TSL `n_forcezombie` model load aborts on a non-NUL-terminated name | unproven | P3 | S | none | — | FIDELITY #15 |
| FID-020 | Build the eleven named fixtures | open | P1 | M | none | — | retro fixture set; every other FID row needs one |
| FID-021 | Three content scans before any code change | open | P1 | S | none | — | retro scan list; gates FID-002, FID-010, FID-015 |

## SYS — game systems

Largely untouched by renderer work and by volume the biggest block in the repo.

| ID | Title | St | Pri | Eff | Owner | Blocked by | Provenance |
|---|---|---|---|---|---|---|---|
| SYS-001 | 60 of 64 effect `.cpp`, 25 header-only effects, 15 of 40 actions are stubs | open | P2 | L | none | — | backlog 6.1 |
| SYS-002 | Serialisation gaps: placeable/door effects and actions, items, `LastDisturbed` | open | P1 | M | none | — | backlog 6.4 |
| SYS-003 | Combat gaps: Dueling, Two-Weapon Fighting, damage effects, history | open | P2 | M | none | — | backlog 6.3 |
| SYS-004 | Action bar missing Force powers, mines, TSL slots | open | P2 | M | none | — | backlog 6.5 |
| SYS-005 | Hardcoded 1.7 m LOS/listener height at six sites | open | P3 | M | none | — | backlog 6.6; collapse to one accessor first |
| SYS-006 | Assorted small fixes (low-HP animations, selectability, listbox CTD, …) | open | P3 | M | none | — | backlog 6.8 |

## TOOL — build, tooling, diagnostics, process

| ID | Title | St | Pri | Eff | Owner | Blocked by | Provenance |
|---|---|---|---|---|---|---|---|
| TOOL-001 | **`tests.exe` heap corruption on a cold cache** | **done** | P0 | M | S1 residue | — | `6d1f79ce7` 2026-08-09: `ComPtr::attach` takes ownership without adding a reference, but `loadModule` returns a module the Slang session keeps in its own cache, so scope exit released a reference this code never held and the session's cached entry was destroyed under it (`shadercompiler.cpp`, attach→assign). That is why it presented as 0xC0000374 or a hang at a point that moved between runs: the corruption was committed long before anything noticed. Twelve consecutive suite runs pass where the same loop before produced two heap-corruption exits and a hang. `9968b918b` fixed a second, separate heap corruption on the same path — SDL3 owns the base path and it was being freed. DESIGN.md, RECORD.md 1.13 |
| TOOL-002 | **GPU timing in `IStatistic`** | open | P1 | M | S0 | — | backlog 2.5; blocks every performance claim in TRC and STR |
| TOOL-003 | Test the editor's Save buttons end to end | open | P0 | S | none | — | backlog 0.2; read-modify-write preserves foreign lines by position |
| TOOL-004 | Rebuild and smoke-test the Debug configuration | open | P0 | S | none | — | backlog 0.4; the checked VMA is the instrument for lifetime bugs |
| TOOL-005 | Exercise the checked-VMA Debug path to completion | open | P1 | S | none | TOOL-004 | DESIGN.md; a Debug capture blocked in `endFrame` |
| TOOL-006 | Suppress wall-clock readouts under `isCaptureRun` | open | P1 | S | none | — | backlog 7.1; the flag exists, nothing at the draw consults it |
| TOOL-007 | Fold the uniform blocks onto the Slang-runtime path | open | P2 | M | S1 | — | DESIGN.md; two schema mechanisms coexist |
| TOOL-008 | Dead shader inventory: 8 of 20 modules have no pipeline consumer | **done** | P2 | S | S1 | — | `901cee651` 2026-08-06 deleted all eight — `pbr_model`, `common`, `shadow`, `particles`, `walkmesh`, `grass` and the two libs only they imported — 954 lines that were compiled and schema-validated on every cold start for nothing; grass and particles still render, through the merged geometry path, and every frame stayed byte-identical. `9accfeddd` took four more unused full-screen entry points and their three bindings. DESIGN.md |
| TOOL-009 | Isolation fixtures: grass cluster placement fails at close range | open | P2 | S | none | — | backlog 7.7; zero clusters at 12 units |
| TOOL-010 | Runtime resolution change reports success and does not take effect | open | **P1** | M | none | — | backlog 4.5; cameras never re-apply projection. **Narrowed 2026-08-09**: the host side is built - `width`/`height` are `Reapply` options (`optionsregistry.cpp:204-206`) and `Engine::applyGraphicsRebuild` resizes the window, flags the swapchain, waits idle and invalidates the render pipelines (`engine.cpp:736-759`). The named residue is exactly what is left: every camera computes `aspect` from the options in its own constructor and never again (`object/camera/thirdperson.cpp:47-48` and the five siblings beside it), and `GUI::_rootOffset` is computed inside `GUI::load` (`gui.cpp:52,63-83`) and read by every control. **P2→P1 the same day**: because the options are `Reapply` rather than `Restart`, `gfx set width … ; gfx apply` now *succeeds and says so* while the view stays stretched and the interface off-centre. Being told a change applied is worse than being told to restart, and it was the latter before the option class moved. Either finish it — a virtual on the camera base the area can call over its cameras, and the scaling switch lifted out of `GUI::load` so it can be re-run — or move the two options back to `Restart` until it is finished. **The second half of that plan is unsafe as written, established 2026-08-09**: two of the four scaling modes do not merely compute `_rootOffset`, they mutate the control tree. `Control::stretch` (`control.cpp:457-470`) multiplies `_extent` in place — `_extent.left = _extent.left * x` — so re-running the switch at the same resolution squares the factor and every later resize compounds on the last. Re-running requires controls to keep their authored extents and re-derive from them, which is a change to `Control`, not an extraction from `GUI::load`. The camera half has no such problem and can be done alone, but doing it alone leaves the interface off-centre while the console still reports success, so it does not on its own retire the reason this is P1. Demoting the two options to `Restart` is the only half-measure that makes the report honest, and it costs the working window-and-swapchain rebuild, which is real |
| TOOL-011 | Retro publishes fewer render targets than PBR | **done** | P2 | M | none | — | backlog 4.6. Verified 2026-08-09: `ScenePipeline::targetEntries` (`scenepipeline.cpp:1234-1272`) enumerates the six G-buffer attachments, the depth and the output with no test on the render mode; the only branch is `_primaryRayMode`, which *adds* the traced channels. The Closed row below asked for exactly this check |
| TOOL-012 | Stale descriptor sets in the render-target viewer across a resize | unproven | P3 | S | none | TOOL-010 | backlog 4.10; never reproduced |
| TOOL-013 | Delete the vendored FSR 3.1.4 SDK (436 MB) | **done** | P3 | S | none | — | backlog 4.3. **Premise corrected 2026-08-09**: nothing was ever vendored - `git ls-files` matching `fsr` returns only `fsrupscaler.h/.cpp`, and no `extern/` FSR drop appears anywhere in history. The 436 MB was the FetchContent clone: FSR2 registers a Cauldron sample framework and a media submodule of demo scene assets that nothing here reads, and whose path lengths fail a Windows checkout outright. `490df9461` takes none of them (`GIT_SUBMODULES ""`, `CMakeLists.txt:179-189`); only `src/ffx-fsr2-api` is consumed |
| TOOL-014 | Ninja generator evaluation | **done** | P2 | M | none | — | backlog 4.4. `2dfde9c87` 2026-08-09: Ninja Multi-Config is the configure, carried by `CMakePresets.json` so it stays one command and resolves the toolchain through `VCPKG_ROOT` rather than a baked path; one tree still serves both configurations, so every `build/bin` path written down still means what it meant. The blocker in this row was the answer, not the obstacle - an ExternalProject inherits the outer generator, and Tracy's vendored libcurl and zstd write an unexpanded `${CONFIGURATION}` into object paths under Ninja, so they ask for single-config Ninja by name |
| TOOL-015 | Measure a clean build before/after the build-speed work | **done** | P2 | S | none | — | backlog 0.6. Measured in `2dfde9c87`: on one 16-core Windows box a full rebuild fell from 267s to 144s and a null build from 17s to 2s. The numbers are that machine's; the two causes are not - the Visual Studio generator charged a fixed toll on every invocation including a no-op, and MSBuild built projects one at a time because CMake does not pass `/m` unless asked. Further build-performance work was explicitly deferred at that point |
| TOOL-016 | Whole-frame parity baseline for danm14ab frame 900 is invalid | open | P2 | S | none | — | backlog 7.3; no replacement figure exists |
| TOOL-017 | A settings tool honest about the three runtime tiers | open | P2 | M | none | — | vulkan-remaining §2 |
| TOOL-018 | Move the `grass` gate from area load to draw time | open | P3 | S | none | — | vulkan-remaining §2b |
| TOOL-019 | Debug view modes (lights, bounds, emissives, object type) | open | P3 | M | none | — | RECORD.md. Two of the four exist as of 2026-08-09: `kDebugEmissive` and `kDebugCategories` (object type) among the twenty views in `debug_view.slang:69-90`, selected by one `debugView` option in every mode. Lights and bounds are the remainder, and neither is a G-buffer channel, so they want overlay geometry rather than a view number |
| TOOL-020 | Rename the registry panel to "Scene viewer" | **done** | P3 | S | none | — | RECORD.md. `8d37449d3` deleted `RenderRegistry` and renamed the panel with it; it is `ImGui::Begin("Objects", ...)` at `editor.cpp:1814`. Landed as "Objects" rather than the "Scene viewer" this row proposed - the name no longer describes machinery that does not exist, which was the point |
| TOOL-021 | Main screen render is cropped rather than scaled | open | P2 | S | none | — | RECORD.md |
| TOOL-022 | Add a Dxun exterior to the K2 fixture set before volumetrics | open | P3 | S | none | TRC-028 | DESIGN.md |
| TOOL-023 | Retire the three obsolete planning documents into a history folder | **done** | P3 | S | none | — | this consolidation; see README provenance table. `9ab94580f` retired all nine, by deletion rather than a history folder: `doc/` now holds `tasks/`, `diagram/` and `_generated/` and nothing else. The decision is answerable because README records where each document's content went and that the originals are recoverable at `63ea1c41^` |
| TOOL-025 | NRD shader blobs can be stale/empty and CMake accepts them | open | **P0** | S | none | — | 2026-08-05; an empty 67-byte `NVSP` blob for `Clear.cs.hlsl` killed path tracing twice and read as an RHI regression; RECORD.md 1.16; recurred 3× — the one stale file is `build/_deps/nrd-src/_Shaders/Clear.cs.spirv.h` at 67 bytes of 31 headers; `rm` it and rebuild regenerates 3,107 bytes. **Half fixed 2026-08-09**: the discarded `VkResult` is checked and throws (`nrddenoiser.cpp:260-262`, and the pipeline layout beside it at `:253`), so an empty blob now names itself instead of reading as an RHI regression. The CMake side is untouched — nothing validates the generated blobs — but the blast radius shrank when `ENABLE_NRD` went default OFF (`490df9461`) |
| TOOL-024 | A startup schema mismatch segfaults instead of naming the field | open | P2 | S | none | — | 2026-08-05, found verifying TOOL-007; `main.cpp:63` catches and logs, but a throw from `VulkanRenderer::init` dies silently — pre-existing, the call site is unchanged; RECORD.md 1.14 |
| TOOL-026 | Neither CI workflow can configure the project | open | P1 | M | none | — | **Windows half addressed 2026-08-09, unverified.** The vcpkg list now installs `imgui[docking-experimental,sdl3-binding,vulkan-binding]`, `vulkan-headers`, `volk`, `vulkan-memory-allocator`, `vk-bootstrap` and `shader-slang`, and drops `sdl2` and `glew`, which nothing has referenced since `df1aa375`. The cache key went v3→v4 in the same edit, because that key gates the install step: leaving it would restore the old package set and skip installing the new one, failing at configure while looking like a bad port name. `--parallel` added since the VS generator does not get `/m` otherwise. **None of this is verified — it cannot be, without a CI run**, and two risks are unresolved: whether a runner with no GPU can pass `ctest` (the suite includes `test/graphics/vulkan/`), and whether FSR2's fetch and shader permutations fit the job's time budget now that `ENABLE_FSR` defaults ON under MSVC. **Linux is untouched and still cannot configure**: apt supplies none of imgui, volk, VMA, vk-bootstrap or Slang as CONFIG packages, so that job needs vcpkg bootstrapped or needs retiring — a decision, not a fix, and the README badge points at it. Original finding: 2026-08-09, documentation sweep. `.github/actions/build-windows/action.yml:25-27` installs `boost-* glm sdl2 sdl3 glew openal-soft libmad ffmpeg wxwidgets gtest`, and `.github/actions/build-linux/action.yml:9-12` the apt equivalents, still including `libsdl2-dev` and `libglew-dev`. `CMakeLists.txt:84-91` requires imgui, VulkanHeaders, volk, VulkanMemoryAllocator and vk-bootstrap, every one of them `REQUIRED`, and none is installed on either runner; there is no `vcpkg.json` manifest to supply them either. So both workflows fail at configure, before compiling anything, and the build badge at the top of README advertises the Linux one. Decide whether CI builds the Vulkan tree — which means the vcpkg Vulkan stack, the Vulkan SDK for `dxc` if NRD is ever on, and a runner that can reach them — or whether it is retired until it can. Leaving it red teaches everyone to ignore it, which is the same failure the `toolkit.exe` link break caused |
| TOOL-027 | Release and RelWithDebInfo share `build/bin` | **done** | P2 | S | none | — | Fixed 2026-08-09: `REONE_BIN_DIR`/`REONE_LIB_DIR` in `CMakeLists.txt:317-329` extend the generator expression to every configuration, and all 23 target sites read them instead of spelling the path out. Release stays `build/bin` and Debug stays `build/debug/bin` so no documented path moved; RelWithDebInfo and MinSizeRel get their own. Verified by building `unerf` in both and finding two binaries. Original finding: 2026-08-09, found in the documentation sweep. Every app target sets `RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}$<$<CONFIG:Debug>:/debug>/bin` (`src/apps/engine/CMakeLists.txt:40`, and the identical line in each sibling), so only Debug gets a directory of its own. AGENTS.md's standing rule — check the binary's timestamp against your edit before trusting a run — cannot tell a Release binary from a RelWithDebInfo one that overwrote it, and the timestamp moves either way. That is precisely the class of mistake the rule exists to catch, so a measurement series can be silently taken against the wrong configuration. Give each configuration its own directory (the generator expression already has the shape), or record the hazard where the rule is stated |
| TOOL-028 | Draw distance has three disagreeing ranges and no consumer | open | P3 | S | none | — | 2026-08-09, found in the documentation sweep. **Three ranges, all verified**: the launcher slider is 32–128 (`src/apps/launcher/frame.cpp:230`), the in-engine editor's is 1–1000 (`src/apps/engine/editor.cpp:1063`), the option itself validates 1–100000 (`optionsregistry.cpp:687`), and the default is 64 (`types.h:26`). A value outside 32–128 set on the command line or in `reone.cfg` is silently clamped the next time the launcher writes the config back (`frame.cpp:580`), because the slider clamps on construction and the save path reads the slider unconditionally. **But the option is inert**, which is the part that decides what to do about it: the value is pushed onto model scene nodes as they are created (`game.cpp:2025`, `creature.cpp:1306`, `placeable.cpp:168` — and commented out in `door.cpp:157`), and `ModelSceneNode::drawDistance()` is never called, so nothing reads it back. The registry says as much in its own description, "draw distance (currently unread)". So this is not "make three numbers agree" but "decide whether draw distance is coming back at all" — and AGENTS.md already records that culling bought nothing here, ~9,000 frustum tests a frame at zero measured frame-time change. If it is not coming back, deleting the option removes three ranges, a dead setter on every model node and two UI widgets. Related, from the same sweep and worth taking together: the config file is parsed with unregistered keys allowed, while the command line is not, so a `reone.cfg` still carrying dead `pbr=`, `backend=`, `fxaa=` and `taajitter=` entries reads as configuring things that no longer exist and says nothing |

---

## Closed by the 2026-08-05 staleness audit

Items the documents still listed as pending that the code shows are done. Kept
so the closure is answerable, not deleted.

| Was | Title | Evidence |
|---|---|---|
| backlog 3.3 | Deforming shadow casters | `shadow_megadraw` draws already-skinned merged geometry — `scenepipeline.cpp:411-416`; `e7a4f5b6` |
| backlog 3.6 | Draw-distance culling in shadow passes | The cited symbol was removed in `db668c2f`; the real residue is FID-012 |
| backlog 4.1 | `toolkit.exe` link failure | Linked in two full Release builds 2026-08-05; binary on disk |
| backlog 4.6 | Retro publishes only its final Output | Both raster modes use the same target enumeration — `scenepipeline.cpp:1234-1272` (was `:969-985` before the file moved). Verified 2026-08-09 and TOOL-011 is now `done`. |
| backlog 4.9 | Compile shaders in the engine with Slang | Landed as S1 — `a6dc6ed1` |
| backlog 9.3 | Bring raster onto the merged buffer | Landed as the G track |
| vulkan-remaining §1 | Render-target viewer empty on Vulkan | `targets()` forwards a populated list — `vulkan.cpp:144-163` |
| vulkan-remaining §2 | Grass gated only at area load | `--grassdensity` is live per frame — `admission.cpp:486-505`; `1b6559aa` |
| vulkan-remaining §5 | Retro is OpenGL-only and forward-rendered | Retro is G-buffer-based on Vulkan — `802ec6c8` |
| vulkan-remaining §6 | Registration rework outstanding | Replaced by direct `GpuScene` collection — `8d37449d` |
| cleanup-plan F-geo | "F2 done, F3 next" | The successors all shipped — `ef6c5850`, `802ec6c8`, `e7a4f5b6` |
| rt-backend §10.2 | Per-mesh BLAS with refits | One scene BLAS since `9b98c37c`; no `refit`/`MODE_UPDATE` in the tree |
| rt-backend §12 | FSR "not started", NRD "not chosen" | Both shipped |
| backlog 8.1–8.6 | Per-mesh loop optimisations | The loop no longer exists — `9b98c37c` |
